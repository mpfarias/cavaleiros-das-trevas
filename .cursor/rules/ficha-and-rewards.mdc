---
description: Ficha, inventário e padrões de ganhos/perdas
globs: src/types/**,src/utils/inventory.ts,src/utils/weapon.ts,src/utils/armor.ts,src/constants/character.ts,src/components/Screen*.tsx,src/App.tsx,src/components/BattleSystem.tsx
alwaysApply: false
---

# Ficha e recompensas

Detalhes canônicos (ganho/perda de itens e atributos): **`game-mechanics.mdc`**.

## Modelo (`src/types`)

- Atributos: `pericia`, `forca` (HP), `sorte` — cada um `{ inicial, atual }`
- Inventário: `bolsa: Item[]` — `arma | armadura | ouro | provisao | equipamento`
- `modificadoresAtivos` + `flags` tipadas; flags ad hoc extras em `cavaleiro:*` no LS
- Validar com `FichaSchema` (Zod)

## Inventário (`utils/inventory.ts`)

Preferir: `adicionarItem`, `removerItem`, `atualizarQuantidade`, `totalOuro`, `validarBolsa`.
Ouro/provisão empilham; arma/armadura substituem a anterior.

## Regras numéricas (resumo)

- Cura/ganho: `Math.min(ficha.X.inicial, ficha.X.atual + n)`
- Perda: `Math.max(0, ficha.X.atual - n)`
- Anti-dupla em mount + `GameAlert` obrigatórios (ver `game-mechanics.mdc`)
---

